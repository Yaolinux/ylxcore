name=kernel-6161
version=6.16.1
source=(http://repo.labo-linux.fr/sources/linux-6.16.1.tar.xz
	http://repo.labo-linux.fr/sources/config_64)

do_build() {
	cd linux-$version

	make mrproper

	cp ../config_64 ./.config

	make
	make modules_install

	cp -iv arch/x86/boot/bzImage /boot/vmlinuz-6.16.1-lfs-12.4-systemd
	cp -iv System.map /boot/System.map-6.16.1
	cp -iv .config /boot/config-6.16.1
	cp -r Documentation -T /usr/share/doc/linux-6.16.1


	install -v -m755 -d /etc/modprobe.d
#	cat > /etc/modprobe.d/usb.conf << "EOF"
	# Début de /etc/modprobe.d/usb.conf

#	install ohci_hcd /sbin/modprobe ehci_hcd ; /sbin/modprobe -i ohci_hcd ; true
#	install uhci_hcd /sbin/modprobe ehci_hcd ; /sbin/modprobe -i uhci_hcd ; true

	# Fin de /etc/modprobe.d/usb.conf

#	EOF
}
