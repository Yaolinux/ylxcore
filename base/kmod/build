name=kmod
version=34.2
source=(https://www.kernel.org/pub/linux/utils/kernel/kmod/kmod-$version.tar.xz)

do_build() {
	cd kmod-$version

	mkdir -p build
	cd       build

	meson setup --prefix=/usr ..    \
            --buildtype=release \
            -D manpages=false

	ninja
	ninja install
}
